[pytest]
addopts = -ra
testpaths = tests
markers =
    contract: public cloudmake launcher contract
    integration: offline integration test spanning Make and a fake provider CLI
    real_github: opt-in integration test cloning pinned public GitHub projects
    live_cloud: opt-in integration test allocating cloud compute
